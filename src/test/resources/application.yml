server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL;
    #url: jdbc:h2:~/test;MODE=MYSQL;
    username: root
    password: test
    driver-class-name: org.h2.Driver
  liquibase: #屏蔽liquibase
    enabled: false